scalar DateTime

type User {
  id: Int!
  name: String!
  email: String!
  password: String!
  isAdmin: Boolean!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type Query {
  user(id: Int!): User
  getUserByEmail(email: String!): User
}
